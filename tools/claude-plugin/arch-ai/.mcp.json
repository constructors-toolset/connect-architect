{
  "mcpServers": {
    "arch-ai": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/archai",
      "args": ["mcp"],
      "env": {
        "LAW_OC": "${LAW_OC:-}",
        "ARCHAI_BOM_DB": "${ARCHAI_BOM_DB:-data/processed/bom/arch-bom.sqlite}"
      }
    }
  }
}
